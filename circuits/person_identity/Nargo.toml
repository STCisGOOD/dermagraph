[package]
name = "person_identity"
type = "bin"
authors = ["Dermagraph"]
compiler_version = ">=1.0.0"
description = "Person-level identity circuit for cross-finger sybil resistance"

[dependencies]
# External Poseidon library for BN254-compatible hashing
poseidon = { tag = "v0.1.1", git = "https://github.com/noir-lang/poseidon" }
